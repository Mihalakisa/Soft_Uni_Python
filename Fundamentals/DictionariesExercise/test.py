person = {'name': 'Mike', 'gender' : 'M', 'age': 19}

# test = person.values()
# test = person.keys()
# test = person.items()
# person.pop('name')
# person.popitem()
# test = person.get('age')
# person.clear()
del person['gender']

# print(person.values())
print(person)